diff --git a/menu/menu_displaylist.c b/menu/menu_displaylist.c
index 8b3de10cbd..2e2f5ca901 100644
--- a/menu/menu_displaylist.c
+++ b/menu/menu_displaylist.c
@@ -10964,12 +10964,12 @@ unsigned menu_displaylist_build_list(
                      MENU_ENUM_LABEL_SAVE_AS_CONFIG,
                      MENU_SETTING_ACTION, 0, 0, NULL))
                count++;
-            if (menu_entries_append(list,
+            /*if (menu_entries_append(list,
                      msg_hash_to_str(MENU_ENUM_LABEL_VALUE_RESET_TO_DEFAULT_CONFIG),
                      msg_hash_to_str(MENU_ENUM_LABEL_RESET_TO_DEFAULT_CONFIG),
                      MENU_ENUM_LABEL_RESET_TO_DEFAULT_CONFIG,
                      MENU_SETTING_ACTION, 0, 0, NULL))
-               count++;
+               count++;*/
             if (!settings->bools.global_core_options && !string_is_empty(sys_info->info.library_name)
                 && menu_entries_append(list,
                   msg_hash_to_str(MENU_ENUM_LABEL_VALUE_CORE_OPTIONS_RESET),
@@ -15369,7 +15369,7 @@ bool menu_displaylist_ctl(enum menu_displaylist_ctl_state type,
                      count++;
 #endif
 
-#if defined(HAVE_LAKKA)
+//#if defined(HAVE_LAKKA)
                if (settings->bools.menu_show_reboot)
                   if (MENU_DISPLAYLIST_PARSE_SETTINGS_ENUM(info->list,
                            MENU_ENUM_LABEL_REBOOT,
@@ -15381,7 +15381,7 @@ bool menu_displaylist_ctl(enum menu_displaylist_ctl_state type,
                            MENU_ENUM_LABEL_SHUTDOWN,
                            PARSE_ACTION, false) == 0)
                      count++;
-#endif
+//#endif
 
                if (!count)
                {
diff --git a/menu/menu_setting.c b/menu/menu_setting.c
index 139ae8e454..338d9e5b57 100644
--- a/menu/menu_setting.c
+++ b/menu/menu_setting.c
@@ -10175,7 +10175,7 @@ static bool setting_append_list(
                parent_group);
          MENU_SETTINGS_LIST_CURRENT_ADD_CMD(list, list_info, CMD_EVENT_MENU_SAVE_CONFIG);
          SETTINGS_DATA_LIST_CURRENT_ADD_FLAGS(list, list_info, SD_FLAG_LAKKA_ADVANCED);
-
+         /*
          CONFIG_ACTION(
                list, list_info,
                MENU_ENUM_LABEL_RESET_TO_DEFAULT_CONFIG,
@@ -10185,6 +10185,7 @@ static bool setting_append_list(
                parent_group);
          MENU_SETTINGS_LIST_CURRENT_ADD_CMD(list, list_info, CMD_EVENT_MENU_RESET_TO_DEFAULT_CONFIG);
          SETTINGS_DATA_LIST_CURRENT_ADD_FLAGS(list, list_info, SD_FLAG_LAKKA_ADVANCED);
+         */
 
          CONFIG_ACTION(
                list, list_info,
@@ -10296,7 +10297,7 @@ static bool setting_append_list(
               parent_group);
 #endif
 
-#if defined(HAVE_LAKKA)
+//#if defined(HAVE_LAKKA)
          CONFIG_ACTION(
                list, list_info,
                MENU_ENUM_LABEL_REBOOT,
@@ -10315,7 +10316,7 @@ static bool setting_append_list(
                &subgroup_info,
                parent_group);
          MENU_SETTINGS_LIST_CURRENT_ADD_CMD(list, list_info, CMD_EVENT_SHUTDOWN);
-#endif
+//#endif
 
          CONFIG_ACTION(
                list, list_info,
